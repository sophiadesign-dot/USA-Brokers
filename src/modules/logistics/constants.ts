/**
 * Intrinsic size hint for `<img>` (layout / CLS). Matches `$size-logistics-icon`
 * in `variables.scss` (12rem with project `html` font-size 4px → 48px).
 */
export const LOGISTICS_ICON_IMAGE_SIDE_PX = 48 as const;
